---
description: Implement tasks using Spec-workflow MCP
---

Implement tasks using Spec-workflow MCP.

- You can bypass "第1原則".
- After completing a task, call `mcp__spec-workflow__manage-tasks`.
- Stop working once you complete a task.

spec path: $ARGUMENTS
